<?xml version="1.0" encoding="UTF-8" ?>

<container xmlns="http://www.symfony-project.org/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.symfony-project.org/schema/dic/services http://www.symfony-project.org/schema/dic/services/services-1.0.xsd">

    <parameters>
        <!-- Controllers -->
        <parameter key="git_wiki.controller.wiki.class">Git\WikiBundle\Controller\WikiController</parameter>
        <parameter key="git_wiki.controller.page.class">Git\WikiBundle\Controller\PageController</parameter>

        <!-- Default pages -->
        <parameter key="git_wiki.page.index">index.rst</parameter>

        <!-- Events -->
        <parameter key="git_wiki.filter.event_name">git_wiki.filter_contents</parameter>
    </parameters>

    <services>
        <!-- Controllers -->
        <service id="git_wiki.controller.wiki" class="%git_wiki.controller.wiki.class%">
            <call method="setContainer">
                <argument type="service" id="service_container" />
            </call>
        </service>

        <service id="git_wiki.controller.page" class="%git_wiki.controller.page.class%">
            <call method="setContainer">
                <argument type="service" id="service_container" />
            </call>
        </service>

    </services>

</container>
